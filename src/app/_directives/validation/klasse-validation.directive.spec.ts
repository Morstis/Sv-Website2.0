import { KlasseValidationDirective } from './klasse-validation.directive';

describe('KlasseValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new KlasseValidationDirective();
    expect(directive).toBeTruthy();
  });
});
